---
import PublicLayout from "@/layouts/PublicLayout.astro";
import { socialImages } from "@/lib/social-images";
---

<PublicLayout
  title="Login"
  description="Sign in to Telecommut."
  robots="noindex,nofollow"
  socialImagePath={socialImages.home}
>
  <section class="glass-panel mx-auto w-full max-w-xl rounded-3xl border p-6 shadow-sm md:p-8">
    <p class="text-xs font-semibold tracking-[0.16em] text-muted-foreground uppercase">Authentication</p>
    <h1 class="mt-3 text-4xl font-semibold tracking-tight">Sign in</h1>
    <p class="mt-3 text-muted-foreground">
      Better Auth endpoint scaffold is active at <code>/api/auth/*</code>. UI form wiring lands next.
    </p>
    <div class="mt-5 flex flex-wrap gap-3">
      <a class="rounded-full bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground hover:opacity-90" href="/">
        Return Home
      </a>
      <a class="rounded-full border px-4 py-2 text-sm font-semibold hover:bg-secondary" href="/help">
        Need Help
      </a>
    </div>
  </section>
</PublicLayout>
