---
import "../../styles/global.css";
import { toAbsoluteUrl } from "@/lib/seo";

interface Props {
  title: string;
  description?: string;
}

const { title, description = "Admin surface scaffold." } = Astro.props;
const canonicalHref = toAbsoluteUrl(`${Astro.url.pathname}${Astro.url.search}`);
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="robots" content="noindex,nofollow" />
    <link rel="canonical" href={canonicalHref} />
    <title>{title} | Telecommut Admin</title>
  </head>
  <body>
    <main class="bg-admin-grid mx-auto min-h-screen p-6 md:p-10">
      <header class="glass-panel mx-auto mb-8 max-w-6xl rounded-2xl border px-5 py-4 shadow-sm">
        <h1 class="text-2xl font-semibold tracking-tight">Admin Control Surface</h1>
        <nav class="mt-3 flex flex-wrap gap-3 text-sm text-muted-foreground">
          <a class="rounded-full border px-3 py-1 hover:bg-secondary" href="/admin/dashboard">Dashboard</a>
          <a class="rounded-full border px-3 py-1 hover:bg-secondary" href="/admin/jobs">Jobs</a>
          <a class="rounded-full border px-3 py-1 hover:bg-secondary" href="/admin/resumes">Resumes</a>
          <a class="rounded-full border px-3 py-1 hover:bg-secondary" href="/admin/users">Users</a>
          <a class="rounded-full border px-3 py-1 hover:bg-secondary" href="/admin/seo">SEO</a>
          <a class="rounded-full border px-3 py-1 hover:bg-secondary" href="/admin/traffic">Traffic</a>
        </nav>
      </header>
      <section class="mx-auto max-w-6xl">
        <slot />
      </section>
    </main>
  </body>
</html>
